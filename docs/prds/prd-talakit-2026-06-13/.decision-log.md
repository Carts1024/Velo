# Decision Log: TalaKit Verify + Debug PRD

## 2026-06-13

- Created initial PRD from `README.md` and `FOLDER_STRUCTURE.md`.
- Used `docs/prds/prd-talakit-2026-06-13/` because the repo does not include `.stellar-build/bmm/config.yaml` or `.stellar-build/scripts/resolve_customization.py`.
- Treated the PRD as fast-path draft mode because the user asked for PRD creation and pointed to README as the source of details.
- Scoped the MVP to "Verify + Debug" based on README hackathon section.
- Treated Stellar Testnet as the MVP target based on README local development and smart contract deployment notes.
- Kept API keys, RPC gateway, request logs, richer event filtering, and error explanation as optional MVP or later-phase work.
- Added open question for product naming because the repo is named TalaKit while README product copy repeatedly uses StellarKit.
- Added assumptions for wallet choice, XDR input, bounded event history, and success metric thresholds where the README did not specify exact requirements.
- Created `ux-design-specification.md` for Phase 1 required screens, focused on a developer-operations dashboard experience for Verify + Debug.
- Updated the UX spec to explicitly reuse `packages/ui/src` components and define a component reuse map for TalaKit Phase 1 implementation.
- Created `phase-1-sprint-plan.md` to break Phase 1 into development-ready sub-phases from readiness through demo hardening.
- Completed Sprint 0 readiness by accepting implementation defaults, adding centralized Testnet config, creating route placeholders, and verifying shared UI imports from `@repo/ui`.

## 2026-06-25

- Completed Project API Keys implementation sprint (designed by Nicole, built by Elliot, reviewed by Code Review).
- Added `apiKeyHash`, `apiKeyPrefix`, and `apiKeyCreatedAt` fields to `projects` database schema, indexing by hash for O(1) verification.
- Implemented Convex mutations `generateApiKey` and `revokeApiKey` with backend SHA-256 hashing.
- Exposed developer APIs at `/api/v1/events`, `/api/v1/transactions/[hash]`, and `/api/v1/webhooks/deliveries` with Bearer and custom header token auth.
- Added API key management panel in the dashboard UI displaying key metadata, a one-time copy reveal warning banner, and available endpoint URLs.

